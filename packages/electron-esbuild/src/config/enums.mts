/*
 * Copyright (c) 2021 Kiyozz.
 *
 * All rights reserved.
 */

export enum TypeConfig {
  esbuild = 'esbuild',
  webpack = 'webpack',
  vite = 'vite',
  typescript = 'tsc',
}

export enum Target {
  main,
  renderer,
}
